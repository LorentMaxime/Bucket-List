{% extends 'back.html.twig' %}


{% block titre %}connexion |{{ parent() }} {% endblock %}


{% block central %}

<div class="col-4">
    <h1>Connexion</h1>
    {{ form_start(formConnexion) }}
    {{ form_widget(formConnexion) }}
    <button type="submit" class="btn btn-success">
        <i class="fas fa-plus"></i>
    </button>
    {{ form_end(formWish) }}
</div>

{% endblock %}